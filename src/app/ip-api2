import { Injectable } from '@angular/core';

export interface IpInfoData {
    countryName: string;
    countryCode: string;
    lang: string;
    postalCode: string;
    city: string;
    ip: string;
}